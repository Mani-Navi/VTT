/**
 * مقادیر ثابت و حساس موتور رندراسیون Konva Canvas
 */
export const DRAG_THROTTLE_MS = 50;
export const MIN_ZOOM = 0.1;
export const MAX_ZOOM = 5;
export const TOKEN_DEFAULT_SIZE = 50;
export const HP_BAR_HEIGHT = 6;
export const GRID_SNAP_THRESHOLD = 0.5;

export const CANVAS_CONSTANTS = Object.freeze({
    DRAG_THROTTLE_MS,
    MIN_ZOOM,
    MAX_ZOOM,
    TOKEN_DEFAULT_SIZE,
    HP_BAR_HEIGHT,
    GRID_SNAP_THRESHOLD,
});